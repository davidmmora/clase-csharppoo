namespace BasculasOOP.Models.Enums;

/// <summary>
/// CONCEPTOS: Enumeraciones (Enums)
/// Una enumeración es un tipo de valor definido por el usuario que consiste en un conjunto de constantes con nombre.
/// Hacen que el código sea más legible y restringen los valores posibles (evitando usar simples strings o números).
/// </summary>
public enum UnidadPeso
{
    Kilogramos,
    Gramos,
    Libras,
    Onzas
}
